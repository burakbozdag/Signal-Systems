The code can be compiled using Python 3.8.2 version.
Syntax compatibility for older versions is not guaranteed.

The GUI is created using Tkinter module.

Open the program with the following command line:
python HW1.py
